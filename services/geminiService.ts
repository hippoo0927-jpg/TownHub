
// AI Generation Service is currently disabled by user request.
// Only manual pixel processing is active.
export const generateArt = async () => {
  throw new Error("AI Generation is disabled.");
};
